interface:
  display_name: "Fix Bug Root"
  short_description: "Root-cause bug fixing workflow"
  default_prompt: "Use $fix-bug-root to find the root cause of this bug and fix it through the right design-level change."
policy:
  allow_implicit_invocation: true
