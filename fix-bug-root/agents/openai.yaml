interface:
  display_name: "Fix Bug Root"
  short_description: "Root-cause bug analysis workflow"
  default_prompt: "Use $fix-bug-root to find and explain the root cause of this bug."
policy:
  allow_implicit_invocation: true
